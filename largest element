#include<stdio.h>
int main ()
{
    int a[5]={10,20,30,40,50};
    int i,max;
max=a[0];
     for(i=0;i<5;i++)
     {
        if(a[i]>max)
        {
            max=a[i];
               
     }
}
     printf("the largest element is=%d",max);
    return 0;
}

